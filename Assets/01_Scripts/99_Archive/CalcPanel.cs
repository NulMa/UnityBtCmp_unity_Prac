using TMPro;
using UnityEngine;

public class CalcPanel : MonoBehaviour
{
    public TextMeshProUGUI calcPanel;

    public bool isTemp1 = true;
    public int Temp1;
    public int Temp2;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
